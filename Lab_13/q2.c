//WAP in C to print the fibbonacci series using recurssion
#include <stdio.h>
int main(){
    int n,a[];
    printf("Enter the number of digits: ");
    scanf("%d", &n);

    fib(n-2);
    return 0;
}

int fib(int n){
    int sum;
    if (n==0){
        return 
    }
}